{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [
        "dist/**",
        "build/**"
      ]
    },
    "development": {
      "cache": true
    }
  }
}